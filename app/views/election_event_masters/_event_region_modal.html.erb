<div id="region_form" class="w3-modal w3-animate-opacity" style="display: block">
    
<%= simple_form_for :region_data , :url => event_district_modal_path, :method => :get, html:{autocomplete:'off'} do |f| %>
    <%= f.input :region_id, label: false do  %>
        <div class="modal-dialog modal-dialog-scrollable" role="document">
            <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Region Selection</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span onclick="document.getElementById('region_form').style.display='none'"
                    >&times;</span>
                </button>
            </div>
            <div class="modal-body">               
                <table class="table table-striped table-condensed table-hover ">
                    <thead class="w3-black">
                        <tr>
                            <th>Region</th>
                            <th> Select</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @regions.each_with_index  do |region,index| %>
                            <tr>
                                <td><%= region.name %></td>
                                <td>
                                    <label>
                                    <input name="region_data[region_id]" type="checkbox"  value="<%= region.region_id %>" />
                                    <span> </span>
                                    </label>
                                </td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
                <input type="hidden" name="val" value="status"/>

            </div>
            <div class="modal-footer">
                <span onclick="document.getElementById('region_form').style.display='none'"
                 class="btn btn-md btn-secondary" >Close</span>
                 <button class=" btn btn-md btn-primary" id="region-data">Next Step</button>


            </div>
            </div>
        </div>
    <% end %>
<% end %>

 
</div>



 <style>
    .bottom-btn{
      margin-top: 5%;
      /*margin-left: 55%;*/
    }
  </style>


  <script>

       $( "#region-data" ).click(function() {
        var url = "<%= event_region_data_url %>";
        var regions = $("input[type=checkbox][name='region_data[region_id]']").val();

        console.log("we are here with region ids = "+regions);

        $.ajax({
            type: "GET",
            url: url,
            dataType: 'script',
            data: {
                region_id: regions
            }, // serializes the form's elements.
            success: function(data)
            {
                $("#event_district_form").html(data);
                // alert("success")
            }
        });
        return false;

    });

  </script>