json.extract! election_event_location, :id, :assigned_code, :election_event_id, :region_id, :constituency_id, :created_at, :updated_at
json.url election_event_location_url(election_event_location, format: :json)
